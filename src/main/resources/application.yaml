spring:
#  sql:
#    init:
#      mode: always
  datasource:
    url: jdbc:postgresql://localhost:5432/university
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 8081